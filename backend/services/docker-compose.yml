version: '3'
services:
  nginx_load_balancer:
    image: nginx
    volumes:
      - './nginx/default.conf:/etc/nginx/nginx.conf'
    ports:
      - '3010:80'
  service1:
    build: ./
    environment: 
      - APPID=3333
  service2:
    build: ./
    environment: 
      - APPID=4444


